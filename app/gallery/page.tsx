import GalleryPage from "@/components/GalleryPage";

export const metadata = {
  title: "Gallery — Lonely",
  description: "摄影作品集",
};

export default function GalleryRoute() {
  return <GalleryPage />;
}
